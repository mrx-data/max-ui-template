{
  "name": "ui-template",
  "version": "0.1.0",
  "private": true,
  "description": "A static UI prompt template gallery with canvas visuals.",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4173",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "typecheck": "tsc --noEmit",
    "lint": "node scripts/validate-site.mjs && npm run typecheck",
    "build": "npm run lint && vite build"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "typescript": "^5.9.3",
    "vite": "^7.2.7"
  },
  "dependencies": {
    "gsap": "^3.15.0"
  }
}
